when adding other dates, see if can add multiple ones

checklist mode 

edit selected task

make repeat every week properly instead of what it does now

When add, delete, edit task should have what selected?

handle leap year

write asumptions for the input

when end timer, make start timer not go to 0